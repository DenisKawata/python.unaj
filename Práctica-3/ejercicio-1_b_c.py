# B) Mencione los errores en los siguientes códigos. Justifique:
    # c)
def suma(par1, par2):
    return(par1+par2)
        suma(12, 10)

# La función suma(par1, par2) devuelve el resultado de la suma de los parámetros par1 y par2 utilizando la palabra clave return. Sin embargo, en la línea suma(12, 10), no se utiliza la función print() para mostrar el resultado de la función en la salida. 

# Para corregir el error y mostrar el resultado de la función suma(par1, par2), puedes utilizar la función print() para imprimir el resultado devuelvo por la funcón. Por ejemplo: print(suma(12, 10)) 

# Con esta corrección, se llamará a la función suma(12, 10) y se mostrará el resultado de la función en la salida.
