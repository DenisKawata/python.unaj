# B) Mencione los errores en los siguientes códigos. Justifique:
    # d)
def suma(par1):
    return(par1+2)
suma(12,10)

# Lafunción suma(par1) está definida para recibir un solo parámetro real par1 y devuelve el resultado de sumar ese parámetro con 2. Sin embargo, en la línea suma(12, 10), se le pasan dos parámetros reales (12, 10), lo cual no coincden con la definición de la función y generará un error.

# Para corregir el error, debes llama a la funcón suma(par1) con un solo parámetro real. Por ejemplo: suma(12)

# Con esta corrección, se llamará a la función suma(par1) con el valor 12 como parámetro real.