# B) Mencione los errores en los siguientes códigos. Justifique:
    # a)
def suma(par1, par2):
    print(par1+par2)
suma()
 
# La función suma(par1, par2) espera recibir dos parámetros formales, par1 y  par2, para realizar la suma de los valores. Sin embargo, en la llamada a la función suma(), no se le pasan los valores necesarios para los parámetros formales, lo que genera un error.

# Para corregir el error, debes proporcionar los valores necesarios para los parámetros formales, par1 y  par2, para que la función suma() pueda realizar la suma de los valores. Por ejemplo: suma(3, 4) llamada a la función suma() con los valores 3 y 4 como parámtros reales.